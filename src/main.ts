import { Editor } from "./classes";

let editor = new Editor();
editor.start();





// to something like tools.setup(editor) and some interface between them like tools and editors are separate, and maybe tools listen to events emitted by editor? with event data